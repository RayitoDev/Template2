'use client';
import { styled } from '@mui/material/styles';

const HeaderCenterArea = styled('div')(() => ({
    display: 'flex',
    flex: '1 1 auto',
    justifyContent: 'center',
}));

export default HeaderCenterArea;