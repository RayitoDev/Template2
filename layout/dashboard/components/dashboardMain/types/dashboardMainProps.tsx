
import { ReactNode } from 'react';
import type MainSectionProps from '@/layout/core/components/mainSection/types';

type DashboardMainProps = {
    children: ReactNode;
    slotProps?: MainSectionProps;
}

export default DashboardMainProps;